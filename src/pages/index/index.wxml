<!--index.wxml-->
<view class="container">
  <map id="ofoMap" 
       latitude="{{latitude}}"  
       longitude="{{longitude}}" 
       scale="{{scale}}"
	   controls="{{controls}}"
       show-location />    
</view>
